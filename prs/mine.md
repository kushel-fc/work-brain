# PRs — mine

PRs Kushel opened, across both repos. Populated on sync.

**Zero open PRs.** His only open PR ([brand-data-pipeline#1890](../sources/github/brand-data-pipeline/open-prs.md), the baldessarini `credential_id` fix) merged 09-16 09:39 UTC. Baldessarini_FEED failed once more shortly after merge (likely deploy lag) then went quiet through the rest of this sync's capture window — see `_meta/earmarks.md` and `dagster-alerts/log.md` for the verification.

His earlier self-closed PR (#2545, GTIN cleanup batch) is still parked in [`archive/prs/product-service.md`](../archive/prs/product-service.md), unrevisited.
